#include <stdio.h>
/**
 *main - point d'entrer du programme
 *
 *decription: affiche le texte  with proper grammar, but the outcome is a piece of art,
 *
 * return : toujours 0 (succes)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
